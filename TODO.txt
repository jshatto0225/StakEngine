-- Create a GUI Renderer in the editor
  -- For the eitor itself

-- Create a GUI Renderer in the runtime
  -- For game GUIs

-- Create a 2D Renderer in the runtime
  -- Add text, lines, and circles
-- Creata a 3D Renderer in the runtime
  -- Both will also be used in the editor for a preview window
  -- Could make a seperate renderer entirely for the editor to support more debug fatures
     but that would be much later down the line

-- Camera System
  -- Perspective proj

-- Audio

-- ECS

-- Make test app without editor

-- Rework Input
  -- Add Gamepad Support

-- Load Images
  -- Support More Image Types

